TKA SMP 2027 migration pack

Contains:
- TKA_SMP_2027_complete.json: questions + image references + extracted exam rules
- app_rules.json: timer/navigation/scoring logic extracted from ActionScript bytecode
- images/: raster assets extracted from Master.swf

Important: Flash vector shapes are not all raster images. The images folder contains extracted bitmap assets; some diagrams may still be vector-only inside SWF and require separate rendering/reconstruction if a question remains visually incomplete.
